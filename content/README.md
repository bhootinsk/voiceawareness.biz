# CMS content (live data)

Files here are **seed / default** content for new installs.

On the production server, admin saves write to `httpdocs/content/` and `httpdocs/data/`. Deploy **does not** overwrite those folders.

To copy live CMS content into this repo, see `docs/EDITING.md` → **Sync CMS content with GitHub**.
